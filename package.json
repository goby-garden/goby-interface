{
	"name": "goby-interface",
	"version": "0.0.1",
	"private": true,
	"main": "src/background.cjs",
	"scripts": {
		"preview": "vite preview",
		"check": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json --watch",
		"vite:dev": "vite dev",
		"vite:build": "vite build",
		"electron:start": "electron .",
		"start": "NODE_ENV=development concurrently \"npm run vite:dev\" \"wait-on http://localhost:5173/ && npm run electron:start\""
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^2.0.0",
		"@sveltejs/kit": "^1.20.4",
		"electron": "^26.0.0",
		"svelte": "^4.0.5",
		"svelte-check": "^3.4.3",
		"typescript": "^5.0.0",
		"vite": "^4.4.2"
	},
	"type": "module",
	"dependencies": {
		"@sveltejs/adapter-static": "^2.0.3",
		"better-sqlite3": "^8.5.1",
		"concurrently": "^8.2.0",
		"wait-on": "^7.0.1"
	}
}
